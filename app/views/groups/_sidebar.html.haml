.chat__group
  - current_user.groups.each do |group|
    .chat__group__link
      = link_to group_messages_path(group), class: :"chat__group__links"  do
        %p.chat__group__name
          = group.name
        %p.chat__group__mmessage
          = side_message(group)
